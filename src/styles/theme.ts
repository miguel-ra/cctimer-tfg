import palette from "./palette";
import breakpoints from "./breakpoints";

const theme = {
  palette,
  breakpoints,
  shape: {
    borderRadius: 6,
  },
  transition: {
    duration: {
      colorMode: 200,
    },
  },
};

export default theme;
